require 'spec_helper'

describe Omniauth::Bigcartel do
  it 'has a version number' do
    expect(Omniauth::Bigcartel::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
